# Завдання 1
def check_name():
    """
    Напишіть програму, яка запитує у користувача його ім'я, якщо воно збігається з вашим, видає певне повідомлення.
    """
    my_name = "ВашеІм'я"  # Замініть "ВашеІм'я" на ваше справжнє ім'я
    user_name = input("Введіть ваше ім'я: ")
    if user_name == my_name:
        print("Привіт, тезка!")
    else:
        print("Привіт, " + user_name)

# Завдання 2
import math

def calculate_cos():
    """
    Напишіть програму, яка обчислює значення функції 𝑦 = cos(3x), де - 𝜋 <= x <= 𝜋 та вводиться з клавіатури. Відповідь подати у радіанах.
    """
    x = float(input("Введіть значення x (в радіанах): "))
    if -math.pi <= x <= math.pi:
        y = math.cos(3 * x)
        print(f"Значення функції y = cos(3x) для x = {x} радіан дорівнює {y}")
    else:
        print("Будь ласка, введіть значення x в діапазоні від -π до π")

# Завдання 3
def solve_quadratic():
    """
    Напишіть програму, яка розв'язує квадратне рівняння 𝑎𝑥² + 𝑏𝑥 + 𝑐 = 0 у дійсних числах. На відміну від аналогічної вправи з минулого уроку, програма повинна видавати повідомлення про відсутність дійсних коренів, якщо значення дискримінанта 𝐷 = 𝑏² − 4𝑎𝑐 негативне, єдине рішення 𝑥 = −𝑏 / 2𝑎, якщо він дорівнює нулю, або два корені 𝑥₁,₂ = (−b±√D) / 2a, якщо він позитивний.
    """
    a = float(input("Введіть коефіцієнт a: "))
    b = float(input("Введіть коефіцієнт b: "))
    c = float(input("Введіть коефіцієнт c: "))

    D = b**2 - 4*a*c

    if D < 0:
        print("Рівняння не має дійсних коренів.")
    elif D == 0:
        x = -b / (2*a)
        print(f"Рівняння має один корінь: x = {x}")
    else:
        x1 = (-b + math.sqrt(D)) / (2*a)
        x2 = (-b - math.sqrt(D)) / (2*a)
        print(f"Рівняння має два корені: x₁ = {x1}, x₂ = {x2}")

# Завдання 4
def calculator():
    """
    Напишіть програму-калькулятор, в якій користувач зможе обрати операцію, ввести необхідні числа та отримати результат. Операції, які необхідно реалізувати: додавання, віднімання, множення, ділення, зведення в ступінь, квадратний корінь, кубічний корінь, синус, косинус та тангенс числа.
    """
    operation = input("Оберіть операцію (+, -, *, /, ^, sqrt, cbrt, sin, cos, tan): ")
    if operation in ['+', '-', '*', '/', '^']:
        num1 = float(input("Введіть перше число: "))
        num2 = float(input("Введіть друге число: "))

        if operation == '+':
            print(f"Результат: {num1 + num2}")
        elif operation == '-':
            print(f"Результат: {num1 - num2}")
        elif operation == '*':
            print(f"Результат: {num1 * num2}")
        elif operation == '/':
            if num2 != 0:
                print(f"Результат: {num1 / num2}")
            else:
                print("Ділення на нуль неможливе")
        elif operation == '^':
            print(f"Результат: {num1 ** num2}")

    elif operation in ['sqrt', 'cbrt', 'sin', 'cos', 'tan']:
        num = float(input("Введіть число: "))

        if operation == 'sqrt':
            if num >= 0:
                print(f"Результат: {math.sqrt(num)}")
            else:
                print("Квадратний корінь з від'ємного числа неможливий")
        elif operation == 'cbrt':
            print(f"Результат: {num ** (1/3)}")
        elif operation == 'sin':
            print(f"Результат: {math.sin(num)}")
        elif operation == 'cos':
            print(f"Результат: {math.cos(num)}")
        elif operation == 'tan':
            print(f"Результат: {math.tan(num)}")
    else:
        print("Невідома операція")

# Завдання 5
def check_even_odd():
    """
    Напишіть програму для перевірки введеного числа на ознаку парності. Якщо число парне/непарне – вивести відповідне повідомлення на екран. Для опису цього алгоритму використовувати тернарний оператор.
    """
    num = int(input("Введіть число: "))
    print("Число парне" if num % 2 == 0 else "Число непарне")

# Завдання 6
def check_day():
    """
    Написати програму визначення днів тижня. Дані про день тижня вводяться користувачем з клавіатури. Якщо будній день - виводити на екран повідомлення "Сьогодні на роботу", у вихідні дні - "Сьогодні вихідний", в інших випадках - "Такого дня не існує".
    """
    day = input("Введіть день тижня: ").lower()
    if day in ['понеділок', 'вівторок', 'середа', 'четвер', "п'ятниця"]:
        print("Сьогодні на роботу")
    elif day in ['субота', 'неділя']:
        print("Сьогодні вихідний")
    else:
        print("Такого дня не існує")


if __name__ == '__main__':
    check_day()